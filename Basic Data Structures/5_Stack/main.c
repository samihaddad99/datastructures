#include <stdio.h>

typedef struct  {
    int value;
    int size;
    int capacity;
} stack;

/*
* push - push to the first available space in the stack (size)
* @param (stack) *stk - stack pointer reference
* @param (int) value - value to add to the stack
*/
void push(stack *stk , int value){

}

/*
* pop - pop the last element in the stack (size)
* @param (stack) *stk - stack pointer reference
* @return (int) - value of the last element in the stack
*/
int pop(stack *stk){

}

int main() {

    return 0;
}